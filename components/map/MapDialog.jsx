import React from 'react'
import ReactDOM from 'react-dom'

function MapDialog(props) {
    return (
        <div className="map-dialog" onClick={() => props.setShowCamp(0)}>
            AA
        </div>
    )
}


export default MapDialog